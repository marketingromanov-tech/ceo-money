@php use App\Support\InvestorPresentation; use App\Support\MoneyFormatter; use App\Support\FeeRulePresentation; @endphp
<div>
    <div class="mb-7"><p class="text-sm font-medium text-purple-300">Операции</p><h1 class="mt-1 text-3xl font-semibold">Выводы</h1><p class="mt-2 text-sm text-[#8f829f]">Дивидендные и капитальные заявки</p></div>
    @if(session('success'))<div class="mb-4 rounded-xl border border-emerald-300/20 bg-emerald-300/10 p-3 text-sm text-emerald-200">{{ session('success') }}</div>@endif
    <div class="mb-4 grid gap-3 sm:ml-auto sm:w-[460px] sm:grid-cols-2"><select wire:model.live="type" class="cursor-pointer rounded-xl border border-white/[.09] bg-[#3c354a] px-4 py-2.5 text-sm"><option value="">Все типы</option><option value="dividend">Дивиденды</option><option value="capital">Капитал</option></select><select wire:model.live="status" class="cursor-pointer rounded-xl border border-white/[.09] bg-[#3c354a] px-4 py-2.5 text-sm"><option value="">Все статусы</option>@foreach(['new'=>'Новая','review'=>'На проверке','approved'=>'Одобрена','paid'=>'Выплачена','cancelled'=>'Отменена','rejected'=>'Отклонена'] as $value=>$label)<option value="{{ $value }}">{{ $label }}</option>@endforeach</select></div>
    <div class="hidden overflow-hidden rounded-[18px] border border-white/[.07] bg-[#3c354a] md:block"><div class="overflow-x-auto"><table class="w-full min-w-[1220px] text-left text-sm"><thead class="border-b border-white/[.07] bg-[#332c42] text-[10px] uppercase tracking-[.06em] text-[#9285a0]"><tr>@foreach(['Инвестор','Тип','Сумма','Комиссия','К выплате','Сеть','Кошелёк','Статус','Дата','Действие'] as $h)<th class="px-4 py-4 font-medium">{{ $h }}</th>@endforeach</tr></thead><tbody class="divide-y divide-white/[.05]">@forelse($withdrawals as $item)@php $address=$item->wallet_address_snapshot;$short=$address&&mb_strlen($address)>12?mb_substr($address,0,6).'…'.mb_substr($address,-4):$address;$action=['new'=>'Рассмотреть','review'=>'Обработать','approved'=>'Выплатить'][$item->status]??null;$statusClass=match($item->status){'new'=>'bg-purple-300/10 text-purple-200','review'=>'bg-amber-300/10 text-amber-200','approved'=>'bg-cyan-300/10 text-cyan-200','paid'=>'bg-emerald-300/10 text-emerald-300','rejected'=>'bg-rose-300/10 text-rose-300',default=>'bg-white/[.06] text-[#a79cac]'};@endphp<tr class="bg-[#3a3348]/45 hover:bg-white/[.04]"><td class="px-4 py-4 font-medium">{{ $item->investor->user->name }}</td><td class="px-4 py-4">{{ InvestorPresentation::type($item->type.'_withdrawal') }}</td><td class="px-4 py-4">{{ MoneyFormatter::format($item->requested_amount) }} {{ $item->currency }}</td><td class="px-4 py-4 text-[#a99dad]">{{ MoneyFormatter::format($item->fee_amount) }} {{ $item->currency }}</td><td class="px-4 py-4 font-semibold text-cyan-200">{{ MoneyFormatter::format($item->net_amount) }} {{ $item->currency }}</td><td class="px-4 py-4">{{ $item->network_snapshot?:'—' }}</td><td class="max-w-40 px-4 py-4 font-mono text-xs" title="{{ $address }}"><span class="block truncate">{{ $short?:'—' }}</span></td><td class="px-4 py-4"><span class="rounded-full px-2.5 py-1 text-[10px] {{ $statusClass }}">{{ InvestorPresentation::status($item->status) }}</span></td><td class="px-4 py-4 text-xs text-[#9285a0]">{{ $item->requested_at->format('d.m.Y H:i') }}</td><td class="px-4 py-4">@if($action)<button wire:click="openAction({{ $item->id }})" class="cursor-pointer rounded-lg px-3 py-2 text-xs {{ $item->status==='approved'?'bg-gradient-to-r from-purple-500/30 to-cyan-400/20 text-white':'bg-cyan-300/10 text-cyan-100' }}">{{ $action }}</button>@endif</td></tr>@empty<tr><td colspan="10" class="px-5 py-12 text-center text-[#9285a0]">Заявок нет</td></tr>@endforelse</tbody></table></div>@if($withdrawals->hasPages())<div class="border-t border-white/[.07] px-5 py-4">{{ $withdrawals->links() }}</div>@endif</div>
    <div class="space-y-3 md:hidden">@forelse($withdrawals as $item)@php $action=['new'=>'Рассмотреть','review'=>'Обработать','approved'=>'Выплатить'][$item->status]??null;@endphp<article class="rounded-[18px] border border-white/[.07] bg-[#3c354a] p-4"><div class="flex justify-between gap-3"><div><h3 class="font-semibold">{{ $item->investor->user->name }}</h3><p class="text-xs text-[#9080ba]">{{ InvestorPresentation::type($item->type.'_withdrawal') }}</p></div><span class="text-xs text-cyan-200">{{ InvestorPresentation::status($item->status) }}</span></div><p class="mt-3 font-semibold">{{ MoneyFormatter::format($item->requested_amount) }} {{ $item->currency }}</p><p class="text-xs text-[#aaa0b3]">К выплате {{ MoneyFormatter::format($item->net_amount) }} {{ $item->currency }}</p>@if($action)<button wire:click="openAction({{ $item->id }})" class="mt-4 w-full cursor-pointer rounded-xl bg-cyan-300/10 py-2.5 text-sm text-cyan-100">{{ $action }}</button>@endif</article>@empty<div class="p-8 text-center text-[#9285a0]">Заявок нет</div>@endforelse</div>
    @if($showActionModal && $selectedWithdrawal)<div x-data="{visible:true}" x-show="visible" x-on:keydown.escape.window="visible=false;$wire.closeAction()" class="fixed inset-0 z-50 flex items-center justify-center bg-[#171020]/80 p-3 backdrop-blur-sm" role="dialog" aria-modal="true"><button aria-label="Закрыть" x-on:click="visible=false;$wire.closeAction()" class="absolute inset-0 cursor-default"></button><section class="relative max-h-[94vh] w-full max-w-2xl overflow-hidden rounded-[22px] border border-white/[.09] bg-[#342c43] shadow-2xl"><header class="flex items-start justify-between border-b border-white/[.07] p-5"><div><h2 class="text-lg font-semibold">Обработка заявки на вывод</h2><p class="text-xs text-[#9080ba]">Заявка #{{ $selectedWithdrawal->id }}</p></div><button x-on:click="visible=false;$wire.closeAction()" class="cursor-pointer rounded-lg p-2 hover:bg-white/[.05]">✕</button></header><div class="max-h-[75vh] overflow-y-auto p-5">
        @error('action')<div class="mb-4 rounded-xl border border-rose-300/20 bg-rose-300/10 p-3 text-sm text-rose-200">{{ $message }}</div>@enderror
        @if($verificationSummary && $selectedWithdrawal->status !== 'new')
            @php $terminalVerification=in_array($selectedWithdrawal->status,['paid','rejected','cancelled'],true); @endphp
            <section class="mb-5 rounded-xl border border-white/[.08] bg-[#2d263a] p-4">
                <div class="flex flex-wrap items-center justify-between gap-2"><div><h3 class="text-sm font-semibold">Проверка выплаты</h3><p class="mt-1 text-xs text-[#9080ba]">Реквизиты и финансовые условия заявки</p></div><span class="rounded-full bg-purple-300/10 px-3 py-1 text-xs text-purple-100">Проверено {{ $verificationSummary['passed'] }}/{{ $verificationSummary['total'] }}</span></div>
                <div class="mt-3 rounded-xl border border-cyan-300/10 bg-cyan-300/[.04] p-3 text-xs"><p class="font-medium text-cyan-100">Проверить реквизиты</p><p class="mt-2 break-all text-[#c4bacd]">Кошелёк: {{ $selectedWithdrawal->wallet_address_snapshot ?: '—' }}</p><p class="mt-1 text-[#9386a1]">Сеть: {{ $selectedWithdrawal->network_snapshot ?: '—' }} · Валюта: {{ $selectedWithdrawal->currency }}</p></div>
                <div class="mt-3 space-y-2">@foreach(\App\Services\WithdrawalVerificationService::KEYS as $checkKey)@php $check=$verificationSummary['checks']->get($checkKey);$system=in_array($checkKey,\App\Services\WithdrawalVerificationService::SYSTEM_KEYS,true); @endphp<div class="rounded-xl border border-white/[.05] bg-white/[.025] p-3"><div class="flex items-start justify-between gap-3"><div><p class="text-sm {{ $check?->status==='passed'?'text-emerald-200':($check?->status==='failed'?'text-rose-200':'text-[#c3bacb]') }}">{{ $check?->status==='passed'?'✓':($check?->status==='failed'?'!':'○') }} {{ \App\Support\WithdrawalVerificationPresentation::label($checkKey) }}</p>@if($check?->checkedBy)<p class="mt-1 text-[10px] text-[#786c87]">Проверил: {{ $check->checkedBy->name }} · {{ $check->checked_at?->format('d.m.Y H:i') }}</p>@elseif($system)<p class="mt-1 text-[10px] text-[#786c87]">Системная проверка</p>@endif</div>@if(!$system&&!$terminalVerification)<div class="flex shrink-0 gap-1">@if($check?->status!=='passed')<button wire:click="passVerification('{{ $checkKey }}')" class="cursor-pointer rounded-lg bg-emerald-300/10 px-2 py-1 text-xs text-emerald-200">Пройдено</button>@endif @if($check?->status!=='failed')<button wire:click="failVerification('{{ $checkKey }}')" class="cursor-pointer rounded-lg bg-rose-300/10 px-2 py-1 text-xs text-rose-200">Ошибка</button>@endif @if($check?->status!=='pending')<button wire:click="resetVerification('{{ $checkKey }}')" class="cursor-pointer rounded-lg bg-white/[.05] px-2 py-1 text-xs text-[#aaa0b3]">Сбросить</button>@endif</div>@endif</div></div>@endforeach</div>
            </section>
        @endif
        @if($actionStep==='confirm')<div class="rounded-xl border border-amber-300/15 bg-amber-300/[.05] p-4"><h3 class="font-semibold">Подтверждение выплаты</h3><dl class="mt-4 grid gap-3 text-sm sm:grid-cols-2">@foreach([['Тип',InvestorPresentation::type($selectedWithdrawal->type.'_withdrawal')],['Инвестор',$selectedWithdrawal->investor->user->name],['Запрошено',MoneyFormatter::format($selectedWithdrawal->requested_amount).' '.$selectedWithdrawal->currency],['Комиссия',MoneyFormatter::format($selectedWithdrawal->fee_amount).' '.$selectedWithdrawal->currency],['К выплате',MoneyFormatter::format($selectedWithdrawal->net_amount).' '.$selectedWithdrawal->currency],['Сеть',$selectedWithdrawal->network_snapshot?:'—'],['Кошелёк',$selectedWithdrawal->wallet_address_snapshot?:'—']] as [$l,$v])<div><dt class="text-xs text-[#9080ba]">{{ $l }}</dt><dd class="mt-1 break-all">{{ $v }}</dd></div>@endforeach</dl><p class="mt-4 text-sm text-amber-100">Проверьте сеть и адрес перед подтверждением выплаты.</p><label class="mt-4 block text-xs">TXID<input wire:model="txid" class="mt-1 w-full rounded-xl bg-[#292235] p-3" placeholder="Необязательно"></label><div class="mt-5 flex justify-end gap-2"><button wire:click="$set('actionStep','details')" class="cursor-pointer rounded-xl px-4 py-2">Назад</button><button wire:click="confirmPayment" wire:loading.attr="disabled" class="cursor-pointer rounded-xl bg-gradient-to-r from-[#9d60eb] to-[#4dbdd5] px-4 py-2 font-semibold disabled:cursor-not-allowed disabled:opacity-50">Подтвердить выплату</button></div></div>
        @else<dl class="grid gap-3 text-sm sm:grid-cols-2">@foreach([['Инвестор',$selectedWithdrawal->investor->user->name],['Тип вывода',InvestorPresentation::type($selectedWithdrawal->type.'_withdrawal')],['Статус',InvestorPresentation::status($selectedWithdrawal->status)],['Дата заявки',$selectedWithdrawal->requested_at->format('d.m.Y H:i')],['Сумма запроса',MoneyFormatter::format($selectedWithdrawal->requested_amount).' '.$selectedWithdrawal->currency],['Комиссия',MoneyFormatter::format($selectedWithdrawal->fee_amount).' '.$selectedWithdrawal->currency],['К выплате',MoneyFormatter::format($selectedWithdrawal->net_amount).' '.$selectedWithdrawal->currency],['Валюта',$selectedWithdrawal->currency],['Сеть',$selectedWithdrawal->network_snapshot?:'—'],['Кошелёк',$selectedWithdrawal->wallet_address_snapshot?:'—'],['TXID',$selectedWithdrawal->txid?:'—'],['Тип комиссии',FeeRulePresentation::economicType($selectedWithdrawal->fee_economic_type_snapshot)],['Плательщик',FeeRulePresentation::payer($selectedWithdrawal->fee_payer?:'')]] as [$l,$v])<div><dt class="text-xs text-[#9080ba]">{{ $l }}</dt><dd class="mt-1 break-all">{{ $v }}</dd></div>@endforeach</dl><div class="mt-5 border-t border-white/[.07] pt-4">@if($selectedWithdrawal->status==='new')<button wire:click="takeToReview" class="cursor-pointer rounded-xl bg-cyan-300/10 px-4 py-2.5 text-cyan-100">Взять на проверку</button>@elseif($selectedWithdrawal->status==='review')<label class="mb-3 block text-xs">Причина отклонения<textarea wire:model="rejectionReason" class="mt-1 w-full rounded-xl bg-[#292235] p-3"></textarea></label><div class="flex flex-wrap gap-2"><button wire:click="approve" class="cursor-pointer rounded-xl bg-cyan-300/15 px-4 py-2.5 text-cyan-100">Одобрить</button><button wire:click="reject" class="cursor-pointer rounded-xl bg-rose-300/10 px-4 py-2.5 text-rose-200">Отклонить</button><button wire:click="cancel" class="cursor-pointer rounded-xl px-4 py-2.5 text-[#aaa0b3]">Отменить</button></div>@elseif($selectedWithdrawal->status==='approved')<button wire:click="requestPaymentConfirmation" class="cursor-pointer rounded-xl bg-gradient-to-r from-[#9d60eb] to-[#4dbdd5] px-5 py-2.5 font-semibold">Выплатить</button>@else<p class="text-sm text-[#9080ba]">Заявка доступна только для просмотра.</p>@endif</div>@endif
    </div></section></div>@endif
</div>
